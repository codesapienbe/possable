server:
  port: 8080

spring:
  profiles:
    active: demo

logging:
  file:
    name: application.log

possable:
  security:
    api-key: "changeme-api-key"
    bearer-token-secret: "changeme-bearer-secret"

app:
  docs:
    enabled: false 